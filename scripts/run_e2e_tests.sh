#!/usr/bin/env bash
# 端到端 RISC-V 执行测试占位（day 4 计划实现）。
# 需等后端输出格式、链接器和模拟器 ABI 确定后再实现。
# 当前阶段请使用 run_smoke_tests.sh 代替。

set -euo pipefail

cat >&2 <<'EOF'
End-to-end RISC-V execution tests are planned for day 4.
They require the backend output format, linker command, and simulator ABI to be finalized.
For day 1, run scripts/run_smoke_tests.sh instead.
EOF

exit 2
