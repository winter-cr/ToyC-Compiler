//短路求值：b为全局变量，side_effect修改b，短路应阻止调用side_effect
int b = 0;

int side_effect()
{
    b = 1;
    return 1;
}

int main()
{
    int a = 1;
    if (a || side_effect())
        return b;       //应返回0（短路未执行side_effect）
    return 0;
}
