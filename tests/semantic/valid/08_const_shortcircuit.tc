// 测试8: 常量逻辑运算和短路
// 目标：验证&&和||的短路逻辑（编译期）
// 短路规则：&&左0则右不执行；||左非0则右不执行
const int ZERO = 0;
const int ONE = 1;
const int A = ZERO && (1 / 1);   // 短路: 左0，结果为0，右不执行即使除零也安全
const int B = ONE || (1 / 1);    // 短路: 左1，结果为1，右不执行

int main(void) {
    return A + B;  // 0 + 1 = 1
}
